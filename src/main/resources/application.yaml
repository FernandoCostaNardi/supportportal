jwt:
  secret: '[F1e0r8n0#1]'

server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/supportportal
    password: root
    username: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
